export function siteMap(sitename: string) {
    if (sitename === 'doublebubblebingo') {
        return 'heart';
    };

    if (sitename === 'ballyuk') {
        return 'starspins';
    };

    return sitename;
};